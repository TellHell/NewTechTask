в IORR + Iqueryable<OR> GetAll (s усатый параметр)

в EFO + Iqueryable<OR> GetAll (s усатый параметр) где OpNa = усатый

в OpManag + Iqueryable<OR> GetAll (s усатый параметр)

+ модель представления для дисплей

в контр + экшн с усатым + if усатый != null => GetAll(усатый) else => GetAll() + запрос

в модель класс для топа + констр + устаый

во вьюшку + экшнлинки для топа если != null


